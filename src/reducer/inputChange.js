let initialState = [];

export const handleChange = (state = "initialState", action) => {
  switch (action.type) {
    case action.type:
      return action;
    default:
      return state;
  }
};

export default handleChange;
